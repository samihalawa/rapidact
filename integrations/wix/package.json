{
  "name": "rapidact-wix-app",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@tanstack/react-query": "^4.36.1",
    "@wix/app-management": "^1.0.0",
    "@wix/design-system": "^1.111.0",
    "@wix/essentials": "^0.1.4",
    "@wix/wix-ui-icons-common": "^3.0.34"
  },
  "devDependencies": {
    "@types/react": "^16.0.0",
    "@types/react-dom": "^16.0.0",
    "@wix/cli": "^1.1.230",
    "@wix/cli-app": "^1.0.0",
    "@wix/sdk-types": "^1.0.0",
    "react": "16.14.0",
    "react-dom": "16.14.0",
    "typescript": "5.8.3"
  },
  "scripts": {
    "prepare:embedded": "node scripts/prepare-embedded.mjs",
    "build": "npm run prepare:embedded && wix app build",
    "release": "npm run prepare:embedded && wix app release",
    "dev": "npm run prepare:embedded && wix app dev",
    "preview": "npm run prepare:embedded && wix app preview",
    "typecheck": "tsc --noEmit",
    "verify": "npm run prepare:embedded && npm run typecheck && node scripts/verify-embedded.mjs"
  }
}
